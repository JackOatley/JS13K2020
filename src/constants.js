
// game settings
export const PREFIX = 'linx';		// whatever the game name will be
export const NODE_DISTANCE = 80;	// in pixels
export const NODE_GROWTH = 0.1;		// how fast new units grow
export const NODE_SPLIT = 16;		// attacking/moving force from base
export const TURN_COOLDOWN = 4;		// in game ticks
export const BALANCE_EFFECT = 0.2;	// combat attack/defence weighting
export const CONNECTIONS_COLOR = 'rgba(187,187,255,0.5)';

// numbers
export const PI = Math.PI;
export const TAU = PI * 2;

/**
 * @enum {number}
 */
export const NODE = {
	BASIC: 0
}
